[project]
name = "ml-algorithms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "einops>=0.8.2",
    "hf>=1.25.1",
    "numpy>=2.5.1",
    "torch>=2.13.0",
]
